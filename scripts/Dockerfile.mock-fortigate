# Mock FortiGate container — enabled in docker-compose via `--profile mock`.
# Stdlib-only Python, so the image is tiny.

FROM python:3.11-alpine
WORKDIR /app
COPY mock-fortigate.py ./
EXPOSE 8443
CMD ["python", "mock-fortigate.py", "--host", "0.0.0.0", "--port", "8443"]
